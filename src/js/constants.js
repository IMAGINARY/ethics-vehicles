export const GLOBAL_SCALE = 0.5;
export const CAR_SCALE = 0.25 * GLOBAL_SCALE;
export const VIEW_SIZE = 1024 * GLOBAL_SCALE;
export const DEFAULT_SPEED = 10 * GLOBAL_SCALE;
export const STREET_X_OFFSET = (1 / 4) * VIEW_SIZE;
export const STREET_Y_OFFSET = (1 / 4) * VIEW_SIZE;
export const STREET_LANE_OFFSET = (1 / 40) * VIEW_SIZE;
export const OFF_SCREEN_LIMIT = (1 / 2 + 1 / 10) * VIEW_SIZE;
